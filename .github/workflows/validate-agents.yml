name: Validate Agents

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    name: Validate Agent JSON Files
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install ajv ajv-formats

      - name: Validate agents
        run: node .github/scripts/validate-agents.js
